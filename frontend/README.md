# Grocery POS — Frontend

Not started yet. Planned stack (per PROJECT_PLAN.md):

- React + Vite + TypeScript + TailwindCSS
- Local web app, served on the same machine, opened via browser (kiosk mode)
- Deployable in local or cloud mode, matching the backend's dual-mode setup

Will be scaffolded in an upcoming phase.
